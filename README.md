# cluster area analysis
A simple script to select clusters within the cell and calculate their area in pixels.

Choose the parameters for binarization with the interactive widget and process the stack, as a result you get a table of cluster areas at each slice and a .tiff with every cluster numbered so you can assess the localisation of each cluster. You will also get a clean .tiff with raw clusters inside of the cell to compare with the binarized version.
